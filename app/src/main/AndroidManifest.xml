<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandorid.graduationhelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraduationHelper">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InputInfoActivity" android:label="명지대학교 졸업 도우미" />
        <activity android:name=".categoryActivity" android:label="명지대학교 졸업 도우미" />
        <activity android:name=".ColiberalActivity" android:label="명지대학교 졸업 도우미" />
        <activity android:name=".LiberalActivity" android:label="명지대학교 졸업 도우미" />
        <activity android:name=".BaseActivity" android:label="명지대학교 졸업 도우미" />
        <activity android:name=".MajorActivity" android:label="명지대학교 졸업 도우미" />
        <activity android:name=".MajorInfoActivity" android:label="명지대학교 졸업 도우미"/>
    </application>

</manifest>